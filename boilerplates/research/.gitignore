# Secrets — never commit real credentials
research.env

# Auth state — contains session tokens
sandbox/auth/*/auth-state.json

# Sandbox working files
sandbox/*/
!sandbox/.gitkeep

# Research assets are committed (screenshots, diagrams, etc.)
